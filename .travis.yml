language: python
python:
  - 3.7 
# command to install dependencies
before_install:
  - sudo apt-get update
  - sudo apt-get install -y p7zip
install:
  - pip install coursera-dl edx-dl


# command to run tests
script:
  - edx-dl -u suman12029@yahoo.co.uk -p Kathmandu123 https://courses.edx.org/courses/course-v1:Microsoft+DAT276x+1T2019a/course/
  - 7za a myapp.zip Downloaded/**/*.mp4
  
deploy:
  provider: releases
  api_key: "GITHUB OAUTH TOKEN"
  file: myapp.zip
  skip_cleanup: true
  on:
    tags: false
